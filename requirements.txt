# EduLens Frontend Dependencies - Minimal for Static Deployment
# Removed GPU/CUDA packages for Netlify compatibility

pillow>=10.0.0
numpy>=1.24.0

# Basic ML dependencies (CPU-only)
scikit-learn>=1.3.0
pandas>=2.0.0

# Image processing (CPU-only)
opencv-python-headless>=4.8.0

# API dependencies (lightweight)
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.5.0

# Development tools
pytest>=7.4.0
black>=23.11.0
flake8>=6.1.0
